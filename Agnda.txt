Agenda 

1: Set Up the github(Repository)
  a)new environment
  b)setup.py
  c)requiremnts.txt


2:Src folder and build the package




























For commit code

git status
git add .
git commit -m "any"
git push -u origin main       -main is branch it will decide